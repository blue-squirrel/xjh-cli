## 搭建一个简单的脚手架

### 安装

```shell
# install it globally
$ npm install -g xjh-cli

# or yarn
$ yarn global add xjh-cli
```

### 使用 

#### Quick Start 

```shell
$ xc create <name> [-f|--force]
```

#### Options

- `-f, --force`: Overwrite if the target exists